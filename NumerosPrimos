# Descobrindo quais os números primos de 0 a 100.
for x in range(101):
    div = 0
    for y in range (1,x+1):
        resto = x % y
        if resto == 0:
            div += 1
    if div == 2:
        print('{} é um número primo '.format(x))
